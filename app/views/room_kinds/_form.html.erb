<%= form_with(model: room_kind) do |form| %>
  <% if room_kind.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room_kind.errors.count, "error") %> prohibited this room_kind from being saved:</h2>

      <ul>
        <% room_kind.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :bed, style: "display: block" %>
    <%= form.number_field :bed %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
